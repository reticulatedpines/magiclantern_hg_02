#Makefile.setup.platform for 1200D 1.0.1

ML_MODULES_SYM_NAME=t5_$(FW_VERSION).sym

CANON_NAME_FIR=CCF15101.FIR
ROMBASEADDR	= 0xFF0C0000

ifeq ($(ML_SRC_PROFILE),generic)
# DryOSmemory map
# RESTARTSTART is selected to be just above the end of the bss
#
RESTARTSTART	= 0x00C80100
else ifeq ($(ML_SRC_PROFILE),installer)
# DryOSmemory map
# RESTARTSTART is selected to be just above the end of the bss
#
RESTARTSTART	= 0x00082900
endif

FIRMWARE_ID = 0x80000327
